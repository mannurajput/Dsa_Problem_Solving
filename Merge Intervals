def merge(intervals):
    intervals.sort()
    res = [intervals[0]]
    
    for s, e in intervals[1:]:
        last = res[-1]
        if s <= last[1]:
            last[1] = max(last[1], e)
        else:
            res.append([s, e])
    return res
